# Source inspection: model waveform chapter

- **Inspected:** 2026-08-21
- **Scope:** Python `wave` API (write mode, sample width/rate/frame parameters); NumPy `sin` and `arange`; Smith sections on sinusoidal/discrete signals and sampling; ISO catalog description for ISO 16.
- **Claims supported:** radians accepted by `sin`; arrays of indices; PCM WAV metadata; discrete sinusoid formulation; A4 convention of 440 Hz.
- **Limitations:** ISO full text was not reproduced; only its public catalog record supports the tuning statement. Library pages are mutable, so access dates are recorded.
- **Inspection note:** Browser search tooling was unavailable (HTTP 401); the cited public pages were inspected directly where available during repository authoring.
